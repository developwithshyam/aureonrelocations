import heroImage from "../../public/images/hero.png";

/** Bundled import so Next.js content-hashes the file when it changes (avoids stale /_next/image cache for same path). */
export const heroImageAsset = heroImage;

export const heroImageAlt =
  "Aureon Relocations moving boxes and furniture in a modern home during a professional move in Abu Dhabi";
